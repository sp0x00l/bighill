# Test Data

`ollama-chat.gguf` is the default fixture for `make test-ollama`.

The script prepares this path from existing local model artifacts when possible. Use `GGUF=/path/to/chat-model.gguf` to run against a specific artifact.
