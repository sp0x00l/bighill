apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-service.fullname" . }}
  labels:
    app: {{ include "tenant-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
    - name: health
      port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
  selector:
    app: {{ include "tenant-service.fullname" . }}
