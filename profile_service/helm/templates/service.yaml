apiVersion: v1
kind: Service
metadata:
  name: {{ include "profile-service.fullname" . }}
  labels:
    app: {{ include "profile-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
    - name: health
      port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
  selector:
    app: {{ include "profile-service.fullname" . }}
