apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-ingestion-service.fullname" . }}-test-connection"
  labels:
    app: {{ include "data-ingestion-service.fullname" . }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "data-ingestion-service.fullname" . }}:{{ .Values.service.healthPort }}/health"]
  restartPolicy: Never
