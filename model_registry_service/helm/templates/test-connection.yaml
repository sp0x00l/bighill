apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "model-registry-service.fullname" . }}-test-connection"
  labels:
    app: {{ include "model-registry-service.fullname" . }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "model-registry-service.fullname" . }}:{{ .Values.service.healthPort }}/health']
  restartPolicy: Never
