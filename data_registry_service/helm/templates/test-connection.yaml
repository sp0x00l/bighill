apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-registry-service.fullname" . }}-test-connection"
  labels:
    app: {{ include "data-registry-service.fullname" . }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "data-registry-service.fullname" . }}:{{ .Values.service.healthPort }}/health"]
  restartPolicy: Never
